# S3 取数代码 — 产出摘要

> 角色：engineering-data-engineer ｜ confidence=0.72

## 产出内容
数据工程师产出了 6 组 ODPS 查询 + pyodps 脚本，覆盖四维度字段。**但置信度仅 0.72，存在多处 schema 假设偏差**，下面列出它在输出里自己标注的"假设"（正是 S4 执行时被纠偏的点）：

## 数据工程师的假设（assumptions，后被 S4 纠偏）

1. ✗ 以为退仓富化表可能缺 `spu_id/sku_id/seller_ue/sku_pay_order_cnt_30d` → 实际字段齐全，不缺
2. ✗ 写脚本时用 miniconda 路径 `/Users/admin/miniconda3/envs/dev/bin/python` → 实际是系统 python3 + pyodps 0.12.5.1
3. ✗ 脚本里 AK 用 `<access_id>`/`<access_key>` 占位 → 实际应从 `/Users/admin/.env` 读
4. ✗ 不知道 sub_order_no 是 BIGINT 而按 STRING 写 `'110206066421203954'` → 实际是 BIGINT
5. ✗ 不知道水位表 `du_risk_trade` 无权限 → 实际无权限（商品参考价缺失）
6. ✗ 多空间 project 名 → projectId 映射未给出 → 实际默认连 `du_risk_training_dev`，各表用 project 前缀直接查

## 结论
S3 的产码方向正确、字段草稿可用，**但真实 schema 必须靠执行层 `o.get_table()` 探查纠偏**。最终可运行的脚本见 `40-代码与结果/取数脚本.py`（已纠偏版）。
